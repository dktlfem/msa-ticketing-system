dependencies {
    implementation project(':common-module')
    runtimeOnly 'com.mysql:mysql-connector-j'
}

bootJar {
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
    archiveFileName = 'app.jar'
}

springBoot {
    mainClass = 'com.koesc.ci_cd_test_app.PaymentApplication'
}