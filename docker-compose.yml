version: '3.8'

x-common-env: &common-env
  TZ: "Asia/Seoul"
  SPRING_PROFILES_ACTIVE: "${SPRING_PROFILES_ACTIVE}"
  SPRING_DATASOURCE_URL: "${SPRING_DATASOURCE_URL}"
  SPRING_DATASOURCE_USERNAME: "${SPRING_DATASOURCE_USERNAME}"
  SPRING_DATASOURCE_PASSWORD: "${SPRING_DATASOURCE_PASSWORD}"
  SPRING_DATASOURCE_DRIVER_CLASS_NAME: "com.mysql.cj.jdbc.Driver"
  SPRING_JPA_DATABASE_PLATFORM: "org.hibernate.dialect.MySQLDialect"
  SPRING_DATA_REDIS_HOST: "${SPRING_DATA_REDIS_HOST}"
  SPRING_DATA_REDIS_PORT: "${SPRING_DATA_REDIS_PORT}"
  SPRING_DATA_REDIS_PASSWORD: "${SPRING_DATA_REDIS_PASSWORD}"
  SPRING_REDIS_PASSWORD: "${SPRING_REDIS_PASSWORD}"

services:
  nginx_proxy:
    image: nginx:latest
    container_name: nginx_proxy
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/localtime:/etc/localtime:ro
    restart: unless-stopped
    networks:
      - app_net

  user-blue:
    image: dktlfem/ci-cd-test:user-app-${BUILD_NUMBER}
    container_name: user-blue
    expose:
      - "8081"
    volumes:
      - /etc/localtime:/etc/localtime:ro
    environment:
      <<: *common-env
      SERVER_PORT: "8081"
    networks:
      - app_net

  user-green:
    image: dktlfem/ci-cd-test:user-app-${BUILD_NUMBER}
    container_name: user-green
    expose:
      - "8082"
    volumes:
      - /etc/localtime:/etc/localtime:ro
    environment:
      <<: *common-env
      SERVER_PORT: "8082"
    networks:
      - app_net

  waitingroom-blue:
    image: dktlfem/ci-cd-test:waitingroom-app-${BUILD_NUMBER}
    container_name: waitingroom-blue
    expose:
      - "8085"
    volumes:
      - /etc/localtime:/etc/localtime:ro
    environment:
      <<: *common-env
      SERVER_PORT: "8085"
    networks:
      - app_net

  waitingroom-green:
    image: dktlfem/ci-cd-test:waitingroom-app-${BUILD_NUMBER}
    container_name: waitingroom-green
    expose:
      - "8086"
    volumes:
      - /etc/localtime:/etc/localtime:ro
    environment:
      <<: *common-env
      SERVER_PORT: "8086"
    networks:
      - app_net

  concert-blue:
    image: dktlfem/ci-cd-test:concert-app-${BUILD_NUMBER}
    container_name: concert-blue
    expose:
      - "8087"
    volumes:
      - /etc/localtime:/etc/localtime:ro
    environment:
      <<: *common-env
      SERVER_PORT: "8087"
    networks:
      - app_net

  concert-green:
    image: dktlfem/ci-cd-test:concert-app-${BUILD_NUMBER}
    container_name: concert-green
    expose:
      - "8088"
    volumes:
      - /etc/localtime:/etc/localtime:ro
    environment:
      <<: *common-env
      SERVER_PORT: "8088"
    networks:
      - app_net

  booking-blue:
    image: dktlfem/ci-cd-test:booking-app-${BUILD_NUMBER}
    container_name: booking-blue
    expose:
      - "8089"
    volumes:
      - /etc/localtime:/etc/localtime:ro
    environment:
      <<: *common-env
      SERVER_PORT: "8089"
    networks:
      - app_net

  booking-green:
    image: dktlfem/ci-cd-test:booking-app-${BUILD_NUMBER}
    container_name: booking-green
    expose:
      - "8090"
    volumes:
      - /etc/localtime:/etc/localtime:ro
    environment:
      <<: *common-env
      SERVER_PORT: "8090"
    networks:
      - app_net

  payment-blue:
    image: dktlfem/ci-cd-test:payment-app-${BUILD_NUMBER}
    container_name: payment-blue
    expose:
      - "8091"
    volumes:
      - /etc/localtime:/etc/localtime:ro
    environment:
      <<: *common-env
      SERVER_PORT: "8091"
    networks:
      - app_net

  payment-green:
    image: dktlfem/ci-cd-test:payment-app-${BUILD_NUMBER}
    container_name: payment-green
    expose:
      - "8092"
    volumes:
      - /etc/localtime:/etc/localtime:ro
    environment:
      <<: *common-env
      SERVER_PORT: "8092"
    networks:
      - app_net

networks:
  app_net:
    driver: bridge