# 부하테스트 분석 리포트

> 생성: 2026-03-22T17:44:06.859705
> 테스트 날짜: 2026-03-22

## 시나리오 요약

| 시나리오 | 판정 | 요청 수 | P50 (ms) | P95 (ms) | P99 (ms) | 에러율 | TPS | Peak TPS | 핵심 지표 |
|----------|------|---------|----------|----------|----------|--------|-----|----------|----------|
| scenario1-rate-limiter | ❌ FAIL | 7,176 | 0.0 | 18.5 | 0.0 | 0% (429) | 130.4 | 129.7 | 차단율 0% |
| scenario2-circuit-breaker | ❌ FAIL | 533 | 0.0 | 79.7 | 0.0 | 77.97% (fallback) | 5.1 | 7.6 | FB 414건 |

### HTTP 상태 코드 분포

- **scenario1-rate-limiter**: 200: 7175
- **scenario2-circuit-breaker**: 200: 117, 503_fallback: 414

### 시간대별 TPS (추정)

**scenario1-rate-limiter** (총 55초):
- baseline: ~2.0 req/s
- burst: ~150.0 req/s
- recovery: ~2.0 req/s

**scenario2-circuit-breaker** (총 105초):
- baseline: ~3.9 req/s
- fault: ~7.3 req/s
- recovery: ~3.9 req/s


## 식별된 병목 (1건)

### 1. 🟡 [MEDIUM] k6_diagnostic

**설명**: scenario1-rate-limiter: Phase 2 (Burst)에서 429 차단율 0.0% — 목표(>50%) 미달

**근거**:
- RequestRateLimiter 필터가 payment-service route에 등록되지 않음
- LocalRateLimiter Bean이 @Primary로 등록되지 않아 다른 RateLimiter가 사용됨
- burst-capacity 설정값이 예상(10)보다 훨씬 큼
- KeyResolver가 IP를 제대로 추출하지 못해 VU별로 다른 키가 생성됨
- 테스트 트래픽(15 VU × sleep 50ms ≈ 300 req/s)이 실제로 burst-capacity를 초과하지 못함

**요약**: Phase 2 (Burst)에서 429 차단율 0.0% — 목표(>50%) 미달

**권장 조치**:
1. application.yml → spring.cloud.gateway.routes[3].filters 에 RequestRateLimiter 존재 확인
1. LocalRateLimiter.java에 @Primary 어노테이션 확인, 로그에서 "local-rate-limiter" 문자열 검색
1. application.yml → gateway.rate-limiter.routes.payment-service.burst-capacity 값 확인
1. SCG 로그에서 X-Forwarded-For 헤더 값 확인. k6 테스트 머신이 프록시 뒤에 있는지 점검
1. 실측 RPS: 129.7 req/s — burst-capacity(10)보다 높은지 확인

---

## k6 자체 진단

### scenario1-rate-limiter

**증상**: Phase 2 (Burst)에서 429 차단율 0.0% — 목표(>50%) 미달
- 원인: RequestRateLimiter 필터가 payment-service route에 등록되지 않음
  - 확인: application.yml → spring.cloud.gateway.routes[3].filters 에 RequestRateLimiter 존재 확인
- 원인: LocalRateLimiter Bean이 @Primary로 등록되지 않아 다른 RateLimiter가 사용됨
  - 확인: LocalRateLimiter.java에 @Primary 어노테이션 확인, 로그에서 "local-rate-limiter" 문자열 검색
- 원인: burst-capacity 설정값이 예상(10)보다 훨씬 큼
  - 확인: application.yml → gateway.rate-limiter.routes.payment-service.burst-capacity 값 확인
- 원인: KeyResolver가 IP를 제대로 추출하지 못해 VU별로 다른 키가 생성됨
  - 확인: SCG 로그에서 X-Forwarded-For 헤더 값 확인. k6 테스트 머신이 프록시 뒤에 있는지 점검
- 원인: 테스트 트래픽(15 VU × sleep 50ms ≈ 300 req/s)이 실제로 burst-capacity를 초과하지 못함
  - 확인: 실측 RPS: 129.7 req/s — burst-capacity(10)보다 높은지 확인

**증상**: 429 응답이 0건 — Rate Limiter가 전혀 동작하지 않음
- 원인: 요청이 payment-service route를 타지 않음 (Path predicate 불일치)
  - 확인: 요청 경로 GET /api/v1/payments/1가 Path=/api/v1/payments/** 에 매칭되는지 확인
- 원인: LocalRateLimiter.isAllowed()에서 항상 allowed=true를 반환하는 버그
  - 확인: LocalRateLimiter.java:55 — count <= config.getBurstCapacity() 조건 확인
- 원인: SCG가 테스트 대상이 아닌 다른 인스턴스로 요청이 갔음
  - 확인: SCG_BASE_URL(http://192.168.124.100:8080)이 실제 scg-app 주소와 일치하는지 확인

### scenario2-circuit-breaker

**증상**: CB 복구 미감지 — HALF_OPEN → CLOSED 전이 미확인
- 원인: payment-app 재시작이 늦어 recovery 단계(75~105s) 내에 HALF_OPEN 전이가 발생하지 않음
  - 확인: 75초 시점에 docker compose start payment-app을 실행했는지 확인. payment-app 기동 시간(~10s) 고려 필요
- 원인: CB OPEN → HALF_OPEN 전이가 recovery 단계 이후에 발생 (waitDuration=30s)
  - 확인: CB가 마지막으로 OPEN된 시점 + 30초가 105초(테스트 종료) 이전인지 확인
- 원인: payment-app 기동 후 health check가 통과하지 않아 HALF_OPEN 요청이 실패 → 다시 OPEN
  - 확인: payment-app actuator/health 응답 확인. 기동 완료까지 약간의 시간 필요

## 다음 단계

1. 위 권장 조치를 severity 순서대로 적용
2. 조치 후 동일 시나리오 재실행
3. `perf-analyzer compare`로 개선 전/후 수치 비교
4. 결과를 Notion/Slack에 공유

---
*Generated by perf-analyzer v0.1.0*